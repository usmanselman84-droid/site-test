# Cookie: сайт закрыт, пока не выберут

## Проблема

Баннер cookie был узкой полоской внизу (`z-index` ниже шапки). Страница при этом блокировала скролл (`lockBodyScroll`), поэтому казалось, что «сайт сломан», а кнопку согласия было не видно.

## Решение (live `theme.css?v=hdr19`, `header-boot.js?v=10`)

Не чёрная стена, а Сочи на фоне: лёгкое затемнение, море и солнце остаются в кадре. Шторка снизу, чтобы не закрывать горизонт.

- заголовок: **«Один шаг — и Сочи откроется»**;
- «Принять» / «Только нужные» крупно; шапка и док не кликаются, пока нет выбора;
- солнце/луна на герое снова живые (время Сочи): раньше слой `hdr16` их прятал.

После кнопки шторка снимается. Закрыть кликом по фону нельзя.
