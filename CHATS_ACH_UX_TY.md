# ty: чаты и достижения удобнее (hdr32)

Только `/opt/sochi-portal-staging`. **py не копировали.**

## Чаты
`messages-boot.js` вставлял второй `<h1>Чаты</h1>` до гидрации — дубль на списке и заголовок «Чаты» поверх треда. Скрипт больше не инжектит шапку; в треде лишние `.messages-head` снимаются. FAB не перекрывает последние диалоги. Акцент моря `#0a7aa8`, не Telegram-синий.

Кэш: `messages-boot.js?v=4`, `theme.css?v=hdr32`.

## Достижения
Чипы статуса (`В работе` / `Есть` / `Б 28/31`) больше не уезжают за край: `flex-wrap`. Активный фильтр лайм, не сирень.

Hard-refresh телефона на ty.
