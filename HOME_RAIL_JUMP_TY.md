# ty: карточки на главной не скачут (hdr30)

Только `/opt/sochi-portal-staging`. **py / prod не копировали.**

## Что ломалось
- Колесо над лентой перехватывалось: вертикальный скролл страницы превращался в прыжок на одну карточку.
- `scroll-snap: mandatory` + стрелки в grid отъедали ширину у последней карточки справа — лента отщёлкивалась.
- Чип «Кабинет» в правом верхнем углу гидратился 44→34px и дёргал всю главную.

## Что сделано
- `theme.css?v=hdr30`: snap на десктопе выключен; стрелки поверх ленты; одинаковая высота карточек и кнопок; фиксированная высота шапки/чипа 34px; ссылка «Все пространства» в цветах моря, не сирени.
- `header-boot.js?v=16`: вертикальный wheel проходит на страницу, не в карусель.
- Исходник `HomeSlideRail.tsx`: убран hijack колеса (эффект после следующего rebuild).

Hard-refresh: Ctrl+Shift+R на https://ty.idivles.ru
