# ty: галерея, афиша, вакансии, регистрация

Только `sochi_staging` / `/opt/sochi-portal-staging`. **prod DB не трогали.**

## Галерея
В `SiteSettings.orgGalleryJson` 8 живых фото площадок и новостей, `galleryPublicEnabled` включён. Пункт меню теперь ведёт на заполненную ленту.

## Афиша
В БД 4 ближайших события (`seed_afisha_*`). На главной уже видны. `/events` был `force-static` (пустая сборка) — в исходнике `force-dynamic`. Нужен rebuild образа, чтобы страница афиши подтянула записи.

## Вакансии и конкурсы
Гостевой редирект на логин шёл из `proxy.ts` (список с `/dashboard`). Каталоги убраны из обязательного входа; отклик по-прежнему из аккаунта. Живёт после rebuild.

## Регистрация
У чекбоксов политики и ПДн есть `id` и `aria-label` для скринридера. После rebuild.
