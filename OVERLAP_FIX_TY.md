# ty: убрать наложения (hdr27)

Карточки сценариев больше **не наезжают** на фото героя (`margin-top` положительный).

Проверены отрицательные margin в theme; `prog-detail__body` без -1.5rem.

py не копировали. Hard-refresh: `theme.css?v=hdr27`.
