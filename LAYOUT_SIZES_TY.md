# Layout / sizes — ty (2026-09-12)

Контур: **только ty**. py не трогали.

## Что было на скриншотах

1. Кабинет (магазин, десктоп): логотип «книга» огромный и перекрывал левое меню. Стрелки — «логотип должен быть в шапке».
2. Эталон шапки: компактная полоса «Молодёжь Сочи + пункты + поиск + Кабинет».
3. Главная на телефоне: шапка ок, но нижний док обрезался / не всегда включался.

## Причина

- Файл `/brand/logo-mark.png` — **716×716**. В CSS стояло `img.site-brand-logo { max-width: none }` и `width/height: 100%`. Если родитель не держал размер, картинка вылезала из шапки на сайдбар.
- `header-boot.js` вешал `has-bottom-nav` в кабинете и на десктопе — из‑за старых правил мог прятать desktop-nav и форсить нижний док.
- hdr40 включал `.yp-bottom-nav { display:flex !important }` в кабинете без медиазапроса.

## Что сделано

| Место | Фикс |
|--------|------|
| Логотип в шапке | жёстко 36×36 (телефон 40×40), `overflow: hidden` на марке, футер 64×64 |
| Шапка кабинета на десктопе | те же ссылки, что на сайте; высота шапки ≤ 4.75rem |
| Нижний док | только ≤860px; на десктопе в кабинете скрыт |
| `has-bottom-nav` | только при `max-width: 860px` |
| Сетка кабинета | колонки 240px + контент, сайдбар sticky |
| Карточки на главной | ширина `min(78vw, 300px)` |

Ассеты: `theme.css?v=hdr59`, `header-boot.js?v=27`.

## Проверка

- Десктоп /dashboard/shop: logo 36×36 в шапке, `.desktop-nav` display:flex, док height≈0
- Телефон /: logo ~40–44px, док 57px внизу экрана
- Жёсткое обновление на телефоне/десктопе

py — только после «одобряю».
