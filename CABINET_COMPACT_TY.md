# ty: кабинет без пустой «страницы в один конец» (hdr34)

Только `/opt/sochi-portal-staging`. **py не копировали.**

## Что было
`/dashboard`: дубль «Мои записи», Список/Календарь при нуле записей, фиолетовая «Открыть историю» рядом с вкладкой «История», вкладка «Общение» вела на заглушку. Карточка тонула в пустом поле.

## Что сделано
- `theme.css?v=hdr34`: скрыты заголовок-дубль, ссылка «Открыть историю», переключатель вида при пустом списке; карточка компактная, без растягивания на весь экран.
- Исходник: «Общение» сразу открывает `/dashboard/messages`; вид Список/Календарь только если есть записи (эффект после rebuild).

Hard-refresh кабинета на ty.
