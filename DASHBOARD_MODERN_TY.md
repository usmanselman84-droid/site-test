# ty: кабинет — группировка и скорость

Только `/opt/sochi-portal-staging`. **py не копировали.**

## Что было не так
- `/dashboard` тянул весь `DashboardClient` (~1.3k строк): профиль, эко, формы, опросы — отсюда лаги.
- Три слоя навигации сразу: рельса хабов + вкладки «Мои записи/Общение» + нижний док.
- На каждую активную запись сразу рисовался QR 112px — тяжело на телефоне.
- CSS с `backdrop-filter` / анимированным фоном в кабинете.

## Что сделано
1. Лёгкий `DashboardOverviewClient` на `/dashboard` — только список записей.
2. `CabinetHubTabs` без дубля вкладок; хабы: Страница / Общение / Дела / Прогресс.
3. QR по кнопке «Показать QR».
4. `theme.css?v=hdr42`: одна визуальная система (лайм/море), без blur, `content-visibility` на карточках, чипы хабов без обрезки, настройки карточками.

Rebuild стейджа нужен для React; CSS уже на живом nginx.
