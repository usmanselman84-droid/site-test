# UX-пакет ty

Только **ty.idivles.ru**. py не трогали.

Версия: **1.6.180**. Тема: `theme.css?v=hdr65`.

## 1. Технический интерфейс (/ops)
- Кнопки площадки: `.btn` вместо фиолетовых `bots-btn`.
- Консоль больше не помечается как `is-admin` (нет сдвига «как у админки без сайдбара»). Класс `is-ops`: без публичной шапки, контент на всю ширину, герой и вкладки по центру.

## 2. MAX.ru
- В статусе: имя бота и текст ошибки API.
- Подсказка: TLS Минцифры, вебхук ~8 ч без 200.
- Кнопка «Проверить MAX» (`pingMax` → `GET /me`).

## 3. Топология
- JSON — иконка в круглой кнопке (не переносится).
- Чипы фильтров в один ряд со скроллом. На узком экране селекты столбиком.

## 4. «Кабинет» в Ops
- Ссылка вела на `/dashboard`, а TECH с `/dashboard` прокси гонит обратно в `/ops` — кнопка «ломалась».
- Теперь «Мой профиль» → `/dashboard/me` (личный кабинет TECH разрешён).

## 5. Безопасность учётки
- Журнал входов: по 8 записей, кнопки Назад/Дальше.

## 6. Куда сходить
- В списке не тянем длинный `description` (быстрее).
- Короткий герой, фото карточек 4:3, фильтр/поиск колонкой на мобилке.

## 7. Документы
- «Смотреть» и «Скачать» в одну строку (`nowrap`).

## 8. Telegram SSO
- После сохранения токена username берётся из `getMe`.
- На странице входа, если username пустой, тоже вызывается `getMe` (кэш 15 с).

## 9. Настройки профиля
- В шторке и десктоп-модалке явная кнопка «Закрыть». Свайп не только за ручку.

## 10. QR пропуска
- Сразу полноэкранный режим «На вход», код крупнее.

## 11. Галерея в панели управления
- Пункт «Галерея» → `/admin/gallery` (редактор фото главной).
