# UX-пакет ty

Только **ty.idivles.ru**. py не трогали.

Версия: **1.6.179**. Тема: `theme.css?v=hdr64`.

## Презентация
- Слайды в БД (`presentationJson`), скрины в `/uploads` — не пропадают после пересборки контейнера.
- Плеер: View Transitions, автопоказ, точки слайдов, URL `#s=N`, заметки (P), автоскрытие панелей.
- Редактор в `/ops` сохраняет заголовок, контакты, заметки докладчика.

## Топология
- Тёмная карта, цвета по ролям, быстрый расклад (сетка вместо O(n²)).
- Кластеры (связные компоненты), лимит узлов, выгрузка JSON.

## Капча
- Proof-of-work (SHA-256) + шумные картинки + honeypot.
- Неверная попытка больше не уничтожает задание. HMAC с timing-safe сравнением.
