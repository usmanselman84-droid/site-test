# Профиль: стиль портала и рабочие «Назад» / «Отмена»

## Почему кнопки не работали

Экран `/dashboard/edit` открывается intercept-модалкой. «Назад» и «Отмена» делали `router.push('/dashboard')`, слот `@modal` не закрывался. Плюс нижний док перекрывал «Отмена» (`z-index` 45 vs шапка/док).

## Что сделано (live `theme.css?v=hdr20`, `header-boot.js?v=12`)

- «Назад» / «Отмена» уводят назад по истории (закрывают шторку).
- Док на этом экране скрыт, кнопки выше по слою.
- Цвета как на сайте: лайм `#afca03`, белые карточки, без iOS-фиолетового «Сохранить».
