# Редактирование профиля — редизайн

Экран `/dashboard/edit` переведён на карточный layout как в современных настройках (iOS Settings / Linear).

## Что изменилось (TEST, ty.idivles.ru)

- Шапка экрана: **Назад / Профиль / Готово** (липкая).
- Блоки-карточки: фото, о вас, интересы, контакты, соцсети, уведомления ботов, галерея.
- Интересы: чипы выбираются на месте (вкл/выкл), выбранные сверху, поиск, «свой вариант» отдельной строкой.
- Нижняя пара **Отмена / Сохранить** не перекрывает поля: отступ над нижним доком.
- Лист Vaul на мобилке растянут на весь экран, без «шторки».

Живой CSS: `theme.css?v=hdr14`. Разметка карточек — после rebuild образа `sochi-staging`.

PROD (`py`) получает только CSS-оверлей; исходник Next на проде не трогали.
