# ТЗ: герой mobile/desktop + навигация

1. Герой: `/covers/photo/sochi-sea-mobile.jpg` (900×1600) + `<picture>` в `HomeHeroMedia` (≤768px). Live CSS `theme.css?v=hdr22`.
2. Кабинет: шапка/футер/док снова (кроме messages/edit).
3. Таб-бар: «Сообщения» → «Чаты», truncate; «Профиль» → `/dashboard/me`.
